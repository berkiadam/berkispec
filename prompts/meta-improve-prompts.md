# Meta-prompt — Prompt fejlesztés

Ez a fájl arra való, hogy egy új AI-sessziót indítsál, amelynek célja a `prompts/` mappa promptjainak felülvizsgálata vagy továbbfejlesztése.

**Használat:** Másold be az alábbi szaggatott vonaltól az AI-nak, és add meg a konkrét fejlesztési célt a végén.

---

Egy **spec-driven development workflow** promptrendszerét fejlesztjük. A `prompts/` mappa az AI-asszisztált szoftverfejlesztési ciklus fázisonkénti instrukcióit tartalmazza, **skillekre** (`prompts/skills-hu/` — fázis-receptek) és **ágensekre** (`prompts/agents-hu/` — specialista subagentek) szervezve. Minden skill egy fázist vezérel — ezeket a promptokat adjuk be egy AI-agentnek, hogy az adott fázist elvégezze.

A rendszer célja: egy-két fejlesztő és egy AI-agent együtt, következetes minőségű, tesztelt szoftvert fejlesszen ciklusonként leszállítható egységekben. A promptok adják az agentnek a keretet, korlátokat, minőségellenőrzési listát és megállási szabályokat.

---

## A workflow felépítése

A **teljes** fejlesztési folyamat 10 fázisból áll (0–9) — 11 skillben, mert a `03` két fázisra hasadt (`03a` kód-terv + `03b` teszt-terv, ugyanabba a `plan.md`-be; 7/k). **Emellett él egy MÁSIK ÚT is** (`bs-quick-flow`) — lásd a lista végén; ha egy szabályt módosítasz, mindig kérdezd meg, igaz-e ott is (7/o).

**A flow ELŐTT (opcionális segédparancs):**
- `bs-brainstorm` — feltáró ötletelés és közös tervezés, amikor még a *mit és hogyan* a kérdés. Perzisztens munkafájl (`.bs-brainstorm/brainstorm-NN-<slug>.md`, gitignore-olt), olcsó **párhuzamos `researcher`** feltárás (BS7), beszélgetési gardek (egy kérdés/kör, 2–3 alternatíva + ajánlás, tilos az igenelés, BS8–BS13). **Nem fázis:** nem változtat státuszt, kódot és a mappán kívül semmit nem ír (BS1). Átadás a flow-nak: `/bs-add-cycles brainstorm: NN` → a `cycle-design-input.md` feltöltése a desztillátummal (BS18); kis feladatnál a **másik útra**: `/bs-quick-flow input: …` vagy `/bs-quick-flow brainstorm: NN` (QF16).

**A flow UTÁN / mellett (opcionális segédparancsok):**
- `bs-cycle-status` — a ciklusok fázis-előrehaladása; **mindkét utat** ismeri (a `plan.md` megléte dönti el, melyik ágat futtatja). Nem fázis, státuszt nem változtat.
- `bs-export-doc` — verziózott PDF export a markdown doksikból (`export/<név>-v<N>.pdf`). Nem fázis, státuszt nem változtat.
- `bs-manual-test-plan` — **kézi tesztterv** a ciklushoz (`specs/cycle-NN-<cycle-name>/manual-test-plan.md`), hogy egy ember végig tudja próbálni a ciklus funkcionalitását: komponens-indítás, tesztadatok, `TG-NN` tesztcsoportok konkrét hívásokkal (`curl` **és** `.http`) és konkrét elvárt eredménnyel, kétirányú `DoD-NN` lefedettség (MT6), és az automata teszteredmények helye. **Kapu — két egyenértékű belépő (MT1):** teljes flow-ban (van `plan.md`) az `analyze-report.md` `PASS` státusza; **egyszerűsített (quick-flow) ciklusban** (nincs `plan.md`, tehát analyze fázis sem volt) a `tasks.md` státusza, pontosan két elfogadott értékkel — `Implementálásra kész` vagy `Kész` (QF8). A fázis nem felderít, hanem **összeszerel**: a `plan.md` `## Környezeti koordináták` (KO1) — quick-flow-ban a `spec.md` technikai vázlata és tesztstratégiája —, a `spec.md` DoD/teszt-szekció és a `conventions.md` riport-táblája alapján; ezért egyben őszinteség-teszt a KO1-re. **Kétmódú (MT3):** `Tervezett` (a tervből, valós kódon nem verifikált) vagy `As-built` (a kódhoz ellenőrizve — eltérésnél a kód nyer, és bekerül a `Változásnapló`-ba). **Nem fázis és nulla visszacsatolás (MT4):** a `00–09` láncot, a státusz-láncot és a `07`/`08`/`09` gépezetét nem érinti, eredményfájl nem készül. Determinisztikus kapu zárja (`manual-test-gate-check.py`, MG1–MG10), fő ágenssel, subagent nélkül (MT5).

**Projekt szintű setup (egyszer fut le):**
- `00` — Projekt inicializálás: `conventions.md` létrehozása (konvenciók, tech stack, portok, merge stratégia)
- `01` — Ciklusok kezelése: `specs/roadmap.md` létrehozása/karbantartása (cikluslista, függőségek, teszt kritériumok)

**Per-ciklus loop (minden fejlesztési ciklusra ismétlődik):**
- `02` — Spec írás: `specs/cycle-NN-<cycle-name>/spec.md` — státusz: `Tervezésre kész`
- `03a` — Kód-terv írás: a `specs/cycle-NN-<cycle-name>/plan.md` **kód-fele** (koordináták, tervezett módosítások, konfiguráció, séma, scope-kapu) — státusz: `Teszt-tervezésre kész`; lezáró kapu: `analyze-gate-check.py --plan-code-only`
- `03b` — Teszt-terv írás: **ugyanannak a `plan.md`-nek** a teszt-fele (`TS-NN` forgatókönyvek, gépi futtatási tábla, környezet-felkészítés, `TA1` adatlapok, spec-lefedettség, regresszió) — státusz: `Task írásra kész`; a belépő kapuja maga futtatja a `--plan-code-only`-t (D5), a lezáró kapuja a teljes `--plan-only`
- `04` — Tasks írás: `specs/cycle-NN-<cycle-name>/tasks.md` — státusz: `Implementálásra kész`
- `05` — Analyze: kereszt-fázisos konzisztencia ellenőrzés (read-only orchestrátor) — `analyze/analyze-report.md` PASS/FAIL **és** `analyze/analyze-task.md`, a triázsban jóváhagyott javítási lista (TR1). Az analízis **minden** fájlja az `analyze/` almappában él (AD1: riport, javítási lista, `slices/`). FAIL esetén önjavító hurok (fixer-subagentek, `max X=3`), per-item túlélés-eszkalációval (TS)
- `06` — Implementálás: kód + `tasks.md` — státusz: `Validálásra kész`; a `test-report/implement/` **hivatalos fázis-mappa** (TR6): mindig a `check-log.md` helye, és ha a `conventions.md` `**Riport-fázisok:**` mezője felsorolja az `implement`-et, a záró állapot teljes riport-készletéé is (ugyanaz a `report-gate-check.py` kapu)
- `07` — Validálás és kódreview: gyors tesztek → statikus réteg (Sonar + `reviewer` subagent) → nehéz tesztek/regresszió → DoD/kapuk (VD13: olcsó → statikus → drága; orchestrátor). **Determinisztikus réteg (VD11/b):** a futtatás/kiértékelés szkriptekkel megy (`run-tests.py` a plan gépi táblájából, `sonar-gate.py` az API-ból, `dod-check.py` bizonyíték-joinnal, `test-substance-check.py` a vacuous tesztekre, `report-gate-check.py` a kör riport-artefaktumaira, `contract-guard.py` a VD3a kapura, `validate-gate-check.py` a kis kapukra, `round-log.py` a naplóra, `failure-counter.py` a leállási korlátokra) — a nyers teszt-log, a Sonar-riport és a `git diff` nem kerül LLM-kontextusba — PASS / FAIL; FAIL esetén önjavító hurok (`implement-fixer` / `review-fixer` = 06 fix-mód, per-item 3 egymást követő / 5 összes bukás + 5 egymást követő FAIL-futás korlát, VD5 eszkaláció 03/02-re)
- `08` — Doc-sync: a `docs-generated/` mappa (system-overview, architecture, CHANGELOG, design-drift, mappa-index + komponens README-k) **és a `specs/test-conventions.md` visszatérő teszt-elvárás regiszter** (TC1, `tc8-gate-check.py`) naprakészen tartása az as-built rendszerhez — terv (`doc-sync-planner` subagent → `doc-sync-plan.md`) → mechanikus végrehajtás → objektív konzisztencia-kapu (DS22); kapu-bukásnál ember-vezérelt javítás (`doc-sync-questions.md`). **Nem** önjavító subagent-hurok.
- `09` — Merge: a ciklus branch beolvasztása (lokális squash vagy PR) — nincs hurok és nincs subagent; a kapuk (státusz, tiszta review, doc-sync) bukása visszairányít a `07`-re vagy a `08`-ra; a merge kézi megerősítéssel (RD8)

**A MÁSIK ÚT (nem fázis, hanem külön recept — a `00–09` lánccal párhuzamosan él):**
- `bs-quick-flow` — **egyszerűsített, háromfázisú flow** kis, jól körülhatárolt feladatokhoz (konfiguráció, egyszerűbb script, kisebb javítás): `spec.md` (`Piszkozat` → `Task írásra kész`) → `tasks.md` (`Piszkozat` → `Implementálásra kész` → `Kész`) → implementáció. **Nincs** `plan.md`, `analyze`, `validate`, `doc-sync` és nincs önjavító hurok — a minőségi kapuk inline futnak; az **egyetlen kötelező kapu-script** az RP1 (`analyze-gate-check.py --paths-only`). A git-konvenciót (fő branch, branch-elnevezés, No-VCS, commit-formátum) a `conventions.md` git-szekciójából olvassa, nem drótozza be (QF1). Ugyanazokat a közös blokkokat hordozza, mint a nagy flow (`path-format`, `artifact-voice`, `dereferencing`, `conventions-change`), a teszt-keményítés viszont **prózában**, kapu nélkül (`QT1`–`QT6`). Túlnövéskor átirányít a `/bs-add-cycles`-ra. **Ha egy keményítő kör a nagy flow-ban tanulságot hoz, kérdezd meg, igaz-e erre az útra is — a 7/o elv erről szól.**

Minden ciklus mappája: `specs/cycle-NN-<cycle-name>/`

---

## A prompt fájlok

> **⚠ A repó KÉTNYELVŰ — a lenti tábla a `-hu` fát nevezi meg, de MINDEN sorának van `-en` párja.**
> A szerkezet **teljesen szimmetrikus** (LG5): nincs suffix nélküli, kitüntetett fa.
>
> ```
> prompts/
> ├── skills-hu/  · agents-hu/  · shared-hu/    # a magyar prompt-nyelvi fa
> ├── skills-en/  · agents-en/  · shared-en/    # az angol — AZONOS fájlnevek, AZONOS szerkezet
> ├── lang/                                     # a PROJEKT-nyelvi tartalom (a két tengely itt találkozik)
> │   ├── status-keys.json                      # szekciónév / mezőnév / státusz szótár (`hu` + `en` szelet)
> │   ├── hu/  ·  en/                           # projekt-nyelvi blokkok + `descriptions.json`
> └── scripts/                                  # nyelvfüggetlen
> ```
>
> **Két nyelvi tengely, egymástól függetlenül:** a **prompt-nyelv** dönti el, melyik `-<lang>`
> fából telepítünk; a **projekt-nyelv** azt, hogy a `lang/<L>/` blokkok és a `status-keys.json`
> melyik szelete kerül be. Mindkettő **build-time** dől el és bedrótozódik (LG2) — a projektben
> semmilyen nyelvi mező nem marad (LG17).
>
> **Amit egy prompt-módosításnál tudni kell:**
> - a **szerkezeti** változást (címsor, kódblokk, INCLUDE-marker, szabály-ID, nyelvi token,
>   imperatívusz-darabszám) **mindkét fán** át kell vezetni — ezt a `lang-parity-check.py` őrzi;
> - az **artefaktum-szekciónevek, mezőnevek és státusz-értékek** a promptban **NEM literálok**,
>   hanem `<sec:…>` / `<field:…>` / `<status:…>` tokenek, amiket a telepítő old fel a
>   `lang/status-keys.json`-ból. **Új szekciónév → előbb kulcs a JSON-ba, csak utána token.**
> - a **user-facing mondatok és az artefaktum-sablonok** nem a promptban élnek, hanem a
>   `lang/<L>/<fájl>.md` horgonyaiban, `<!-- INCLUDE:lang/<fájl>.md#<horgony> -->` markerrel
>   behivatkozva — ezeket **mindkét nyelven** szerkeszteni kell.

| Fájl | Fázis | Bemenet | Kimenet |
|------|-------|---------|---------|
| `prompts/skills-hu/brainstorm.md` | *(nem fázis — a flow előtt)* | téma szabad szöveggel / `folytassuk a NN-est` | `.bs-brainstorm/brainstorm-NN-<slug>.md` (Cél · Tények forrással · Alternatívák · Döntések · Nyitott kérdések · Javasolt ciklus-vágás · Napló) — átadás a `01`-nek (BS18) |
| `prompts/skills-hu/manual-test-plan.md` | *(nem fázis — az 05 után bármikor)* | ciklus mappa (opcionálisan `mód: tervezett` / `mód: as-built`) | `manual-test-plan.md` (Környezet és indítás · Tesztadatok · Automata tesztek · `TG-NN` kézi tesztcsoportok · Nem kézzel tesztelhető · Lefedettség · Változásnapló) — előfeltétel: `analyze-report.md` = `PASS`, **vagy** quick-flow ciklusban (nincs `plan.md`) a `tasks.md` státusza `Implementálásra kész` / `Kész` (QF8); kapu: `manual-test-gate-check.py` |
| `prompts/skills-hu/quick-flow.md` | *(nem fázis — a MÁSIK ÚT: egyszerűsített, háromfázisú flow)* | feladat leírása (`input: …`) vagy `brainstorm: NN` | `spec.md` (`Piszkozat` → `Task írásra kész`) + `tasks.md` (`Piszkozat` → `Implementálásra kész` → `Kész`) + implementáció — nincs `plan.md`, nincs analyze/validate/doc-sync és nincs önjavító hurok; egyetlen kötelező kapu-script az RP1 (`analyze-gate-check.py --paths-only`); a `path-format` / `artifact-voice` / `dereferencing` / `conventions-change` shared-blokkokat beemeli; `QF-`/`QT-` azonosítók |
| `prompts/skills-hu/cycle-status.md` | *(segédparancs — bármikor)* | ciklus neve vagy útvonala (opcionális) | a ciklusok fázis-előrehaladása; **mindkét flow-t** ismeri (a `plan.md` megléte dönti el, melyiket), a lightweight ág a `spec.md` + `tasks.md` státusz-mezőiből olvas (a régi `task.md` visszafelé kompatibilisen) — `cycle-status.py` |
| `prompts/skills-hu/export-doc.md` | *(segédparancs — bármikor)* | markdown fájl(ok), üresen a `docs-generated/` két fő doksija | `export/<név>-v<N>.pdf` — `export-doc.py`; nem fázis, státuszt nem változtat |
| `prompts/skills-hu/00-init-project.md` | Projekt init | Projekt leírás | `conventions.md` |
| `prompts/skills-hu/01-add-cycles.md` | Ciklusok kezelése | HLD/LLD vagy leírás (vagy `brainstorm: NN`) | `specs/roadmap.md` (`Kész`) + `cycle-design-input.md` (opcionális, CD1 / BS18) + `spec-input-from-prev.md` (MINDIG, üresen is — IP1); ciklusszám a **BQ2** formulával, branch-nyitó preflight (`git-preflight.md`, PW1–PW5) |
| `prompts/skills-hu/02-write-spec.md` | Spec | Roadmap + ciklus neve (+ `cycle-design-input.md`, `docs-generated/system-overview.md` mint current truth) | `spec.md` (`Tervezésre kész`) + `spec-questions.md` + a következő fázisoknak szóló `*-input-from-prev.md` (IP1) |
| `prompts/skills-hu/03a-write-code-plan.md` | Plan — kód-fél | `spec.md` | a `plan.md` kód-szekciói (`Teszt-tervezésre kész`): `Környezeti koordináták` (KO1), `Tervezett módosítások` céllal (WY1), `Konfiguráció` (KF1), `Séma-artefaktumok`, `Fordított lefedettség` (SC1). Teszt-szekciót NEM nyit. |
| `prompts/skills-hu/03b-write-test-plan.md` | Plan — teszt-fél | a `plan.md` kód-fele + a spec teszt-szekciója és `DoD`-ja | ugyanannak a `plan.md`-nek a teszt-szekciói (`Task írásra kész`): a `### Teszt-forgatókönyvek` (`TS-NN`, TS1–TS8), a `Fázis` oszlop (PH1) és a `### Gépi futtatási tábla` (TP4); a forgatókönyvek tervezését a `shared-hu/test-scenario-design.md` receptje (TD0–TD7) vezeti; `TA1` adatlapok, `TI1` névtér, `TS7` spec-lefedettség. |
| `prompts/skills-hu/04-write-tasks.md` | Tasks | `plan.md` | `tasks.md` (`Implementálásra kész`) |
| `prompts/skills-hu/05-analyze.md` | Analyze | ciklus mappa | `analyze/analyze-report.md` (PASS/FAIL — diagnózis és audit-nyom) + `analyze/analyze-task.md` (a **triázsban** jóváhagyott javítási lista, TR1 — a fixerek kizárólag ezen dolgoznak); minden fájl az `analyze/` almappában (AD1). Mechanikus kapu + négy párhuzamos diagnoszta-kör (`analyzer` × 3 hatókör, `analyzer-exec`); FAIL → önjavító hurok (fixer-subagentek, `max X=3`, iterációnként egy analyzer-kör), per-item túlélés-eszkalációval (TS, `AF-NN`/`AC-NN`/`AN-NN`/`AX-NN` azonosítókon) |
| `prompts/skills-hu/06-implement.md` | Implementálás | `tasks.md` | Kód + `tasks.md` (`Validálásra kész`) + `test-report/implement/` (`check-log.md`, és riport-fázis esetén a teljes riport-készlet — TR6); a task listát **egy futásban** dolgozza fel (IM1) |
| `prompts/skills-hu/07-validate.md` | Validálás + kódreview | `spec.md`, `plan.md`, `tasks.md`, cycle diff | `test-report/validation-report.md` + `test-report/code-review.md` — FAIL → önjavító hurok (`implement-fixer` / `review-fixer`, 3-próba korlát, VD5 eszkaláció) |
| `prompts/skills-hu/08-doc-sync.md` | Doc-sync | ciklus mappa + `docs-generated/` + `specs/test-conventions.md` | konzisztens `docs-generated/` (system-overview, architecture, CHANGELOG, design-drift, README) + a `specs/test-conventions.md` regiszter karbantartása (TC1 promóció / elavult tétel törlése, `tc8-gate-check.py`) + `doc-sync-plan.md` — terv (`doc-sync-planner`) → végrehajtás → objektív kapu (DS22, `ds22-gate-check.py`); kapu-bukás → ember-vezérelt javítás (`doc-sync-questions.md`) |
| `prompts/skills-hu/09-merge.md` | Merge | ciklus mappa, `conventions.md` | merged branch / PR + lezárt roadmap — nincs hurok és nincs subagent; bukó kapu → vissza a 07-re vagy a 08-ra |

A **tizenegy** specialista subagent a `prompts/agents-hu/` alatt (mindegyiknek van `-en` párja és `gemini-agent/<név>/agent.json` tükre — a tükröt a `sync-gemini-agents.py` generálja):

- **diagnoszták (read-only):** `reviewer.md` (07 — kód-diagnózis; **inkrementálisan ír**, RV-INC), `analyzer.md` (05 — **szemantikai** diagnózis, 1–5. kategória, hatókör-paraméterrel három párhuzamos körben), `analyzer-exec.md` (05 — **végrehajthatósági** diagnózis, 6. kategória; az `analyzer`-körökkel párhuzamosan fut), `researcher.md` (03a/03b Mód A; 00/01/02/06 + `bs-brainstorm` Mód B), `doc-sync-planner.md` (08 — doc-sync tervkészítő);
- **mechanikus végrehajtó:** `test-runner.md` (07 — tesztek/Sonar/E2E futtatása és **tényszerű** összegzése; PASS/FAIL-ról **nem** dönt, azt az orchestrátor teszi);
- **fix-mód belépők:** az 05 hurkáé `spec-fixer.md`, `plan-fixer.md`, `tasks-fixer.md`; a 07 hurkáé `implement-fixer.md` (teszt/Sonar/DoD) és `review-fixer.md` (review-findingok), `## Validációs javítások` ill. `## Review javítások` bemenettel.

**A quick-flow is hív hármat** — `researcher`, `analyzer`, `reviewer` —, de **kontraktus-helyettesítésekkel**: a skill mondja meg, mi kerül a hiányzó `plan.md` és a hiányzó hatókör-szelet helyére, és hova íródik a `code-review.md` (QF18). Az agent-promptok **törzse ettől változatlan**, csak a `called_by` listájuk bővült.

**A fix-mód belépők két megvalósítása** (mindkettő logika-duplikáció nélkül): a 02/03/04 fixerek promptja **önhordó** — a fix-mód szekció és a fázis minőségi kapuja a `prompts/shared-hu/{fix-mode,quality-check}-*.md` fájlokból **build-time beemelődik** a skillbe és a wrapperbe is, így a fixer **nem olvas fázis-skillt** (D13). A 06 fix-módját használó `implement-fixer`/`review-fixer` viszont még a klasszikus úton, a `06-implement.md` Fix-mód szekciójának beolvasásával delegál.

A repó gyökerében lévő `README-HU.md` „Indító prompt (copy-paste)" szekciója tartalmazza a felhasználónak szánt indító prompt blokkot.

**Telepítés és scriptek.** A támogatott telepítési út: `./install.sh` (vagy `install.ps1`) →
`prompts/scripts/install-helper.py`. A `prompts/scripts/init-project.sh` **elavult** (LG19):
szimlink-alapú alternatíva, amit soha nem használtunk — ne hivatkozz rá, és ne fejleszd tovább.

### Determinisztikus kapu- és segéd-scriptek (`prompts/scripts/`)

A keret alapelve: **ami gépiesen eldönthető, azt script döntse el, ne LLM.** Prompt-módosításnál
mindig kérdezd meg, (a) van-e olyan script, amely a szabályt **már méri** — akkor a próza csak
másodlagos —, és (b) ha egy szabályt vagy egy artefaktum-szerkezetet átírsz, **melyik script
olvassa** azt (mert az némán rossz cellát fog használni; lásd 7/m).

| Script | Ki futtatja | Mit mér / mit csinál |
|---|---|---|
| `analyze-gate-check.py` | `02`/`03a`/`03b`/`04` lezárás (M), `05` minden kör, `bs-quick-flow` (`--paths-only`) | a mechanikus kereszt-fázisos kapu. Módok: `--plan-code-only`, `--plan-only`, `--paths-only` (RP1), `--emit-slices` (SH1). **Generálja** a riport lefedettségi tábláit és a leltárt |
| `run-tests.py` | `06` (riport-fázisban), `07`, `test-runner` | a plan **gépi futtatási táblájából** futtat, fix oszlop-pozíciókkal (TP4/b); `exit 4` = környezet-hiba (EV5) |
| `test-substance-check.py` | `06`, `07` | vacuous teszt-törzs (TB1) és a `[CHECK]` szelektor létezése (TB2) |
| `dod-check.py` | `07` (a `02`/`03b` csak **hivatkozik** rá: ez olvassa majd a `DoD-NN`-eket és a `TS-NN` adatlapokat) | `DoD-NN` ↔ bizonyíték join; a `skipped` **nem** bizonyíték (SK1) |
| `report-gate-check.py` | `06` (TR6 riport-fázis), `07`, `test-runner` | a kör riport-artefaktumai a `conventions.md` TR3 táblája szerint; TR7 frissesség, EV6 cél-host, `RL1`/`RL2` REST-napló join |
| `validate-gate-check.py` | `06`, `07`, `reviewer` | a kör „kis kapui": riport-fejléc, befejezetlen review (RV-INC), marker-rend |
| `sonar-gate.py` | `07` | Sonar Quality Gate az API-ból, a `conventions.md` küszöbeivel |
| `contract-guard.py` | `07` | VD3a: a javítás nem írhatja át a tesztelt szerződést |
| `round-log.py` | `07` | kör-napló és kör-mappa (`round-NN`) kezelése; `TELJES`/`KÖNNYŰ` körtípus |
| `failure-counter.py` | `07`, `reviewer` | per-item és per-kör bukás-számlálók a hurok leállási korlátaihoz |
| `ds22-gate-check.py` | `08` | DS22 objektív doc-konzisztencia (megszűnt azonosító, mappa-index halmaz, coverage-marker) |
| `tc8-gate-check.py` | `08`, `doc-sync-planner` | a `specs/test-conventions.md` regiszter TC8 kapuja |
| `manual-test-gate-check.py` | `bs-manual-test-plan` | MG1–MG10 (`TG-NN` alak, konkrét elvárt eredmény, kétirányú lefedettség) |
| `cycle-status.py` | `bs-cycle-status` | **mindkét utat** ismeri: a `plan.md` megléte dönti el, teljes vagy egyszerűsített ág |
| `export-doc.py` | `bs-export-doc` | verziózott PDF export (pandoc + `mermaid-filter` + xelatex) |
| `worktree-setup.py` | `00`/`01` (`shared/git-preflight.md`) és `01`/`06` (`shared/parallel-cycles.md`) — PW4 | a gitignore-olt agentic eszköz-mappák pótlása egy párhuzamos ciklus worktree-jében (idempotens, `--dry-run`-olható) |

**Repó-karbantartó eszközök** (a telepítő ezeket **nem** másolja a célprojektbe):
`install-helper.py` (build-time INCLUDE- és token-feloldás, öt platform), `lang-parity-check.py`
+ `lang_keys.py` (nyelvi paritás, §11), `sync-gemini-agents.py` (az `agent.json` tükrök),
`acceptance-check.sh` (a kétnyelvűsítés elfogadási sora, 16.1–16.5).

### Közös blokkok (`prompts/shared-{hu,en}/`) — egy szabály, egy hely

Ezek **nem** önálló skillek: a telepítő build-time **inline** beágyazza őket a hivatkozó skill/agent
telepített változatába a `<!-- INCLUDE:shared/<fájl> -->` marker helyére (rekurzívan, 5 mélységig).
**Ha egy itt definiált szabályt módosítasz, egy helyen szerkeszd** — korábban több szabály három
helyen, eltérő tartalommal élt (ez az RP1 tanulsága).

| Blokk | Mit rögzít | Ki emeli be |
|---|---|---|
| `context-check.md` | a fázis-eleji kontextus-ellenőrzés | minden fázis-skill + `cycle-status`, `manual-test-plan`, `quick-flow` (a `brainstorm` és az `export-doc` nem) |
| `python-cmd.md` | a platform-scripts mappa és a `python3` hívás alakja | `03a`, `03b`, `04`, `05`, `07`, `08`, `manual-test-plan`, `export-doc` + a fixer-wrapperek |
| `path-format.md` | RP1 útvonal-konvenció + a `--paths-only` kapu | `quick-flow`, `brainstorm`, és a négy `quality-check-*` blokkon át a `02`/`03a`/`03b`/`04` + fixereik |
| `artifact-voice.md` | AV1 — az artefaktum az EMBERNEK szól, nem az ágensnek | `02`, `03a`, `03b`, `04`, `quick-flow` |
| `dereferencing.md` | KX2 — a bemenet absztrakciós szintje nem a terv szintje | `03a`, `03b`, `plan-fixer`, `quick-flow` |
| `spec-artifact-transfer.md` | KX3 — a spec kidolgozott artefaktumai csonkítás nélkül mennek át | `03a`, `03b`, `plan-fixer` |
| `plan-self-contained.md` | a plan önhordó: tilos a „lásd a spec-et" jellegű hivatkozás | `03a`, `03b` |
| `plan-section-ids.md` | a `[P-…]` azonosítók formátuma és egyedisége | `03a`, `03b` |
| `test-scenario-design.md` | TD0–TD7 — a teszt-forgatókönyv generáló receptje | `03b`, `plan-fixer` |
| `conventions-change.md` | GC1 — a kapu-konfiguráció együtt mozog a struktúrával | `03a`, `quick-flow` |
| `input-from-prev.md` | IP1 — a fázisok közti `*-input-from-prev.md` átadás | `01`, `02`, `03a`, `03b`, `04`, `07` |
| `git-preflight.md` | a branch-nyitó preflight: friss/tiszta `main`, worktree-helyzet és resume-felismerés (BD1/BD3/BD5, BQ2–BQ4, PW1–PW5) | `00`, `01` |
| `parallel-cycles.md` | párhuzamos ciklusok worktree-ben (PW3/B, PW4) | `01`, `06` |
| `phase-commit.md` | a fázis-záró commit + a PE1 fázishatár | `02`, `03a`, `03b`, `04`, `05`, `07`, `manual-test-plan` |
| `quality-check-{spec,plan-code,plan-test,tasks}.md` | a fázis lezárási minőségi kapuja | az adott fázis **és** a fixer-wrapperje (D13) |
| `fix-mode-{spec,plan,tasks,implement}.md` | a fix-mód belépő szerződése | az adott fázis és a fixer-wrapper |
| `questions-tasks.md` | a `*-questions.md` kérdés-formátum | `04`, `tasks-fixer` |
| `review-checklist.md` | a kódreview szempontlistája — **mindkét** végrehajtási ágon jelen kell lennie (RV-FB1) | `07`, `reviewer` |

**⛔ KÉTNYELVŰ REPÓ — KÖTELEZŐ KÉZI KAPUK MINDEN COMMIT ELŐTT.** A promptok **két
prompt-nyelvi fában** élnek (`prompts/skills-hu/` ↔ `prompts/skills-en/`, ugyanígy
`agents-*` és `shared-*`), a projekt-nyelvi blokkok pedig a `prompts/lang/<nyelv>/`
alatt. **Nincs CI és nincs pre-commit hook**, tehát a két fa szinkronját semmi nem
őrzi automatikusan — ezt a két scriptet **kézzel kell lefuttatni**:

```bash
python3 prompts/scripts/lang-parity-check.py      # nyelvi paritás (§11) — default mód
python3 prompts/scripts/sync-gemini-agents.py --check   # a gemini agent.json tükrök
```

- **Ha az egyik nyelvi fát szerkeszted, a másikat is szerkeszd** — a paritás-kapu a
  szerkezeti eltérést (címsor, kódblokk, INCLUDE-marker, szabály-ID, nyelvi token,
  imperatívusz-darabszám) megfogja, a **jelentés**-eltérést nem: az emberi review dolga.
- A kapu **két üzemmódú**: a napi, commit előtti futás a **defaultot** használja (a
  féloldalas fájlok WARN-ok), a PR zárása és a végső elfogadás a **`--strict`**-et
  (ott a teljes fájlhalmaz-paritás is kötelező).
- Mindkét script **repó-karbantartó eszköz**: a telepítő szándékosan **nem másolja**
  őket a célprojektbe.

---

## Tervezési elvek, amelyek minden promptban érvényesülnek

**1. Fáziskapu (előfeltétel-ellenőrzés)**
Minden prompt első lépése: beolvassa az előző fázis dokumentumának státuszát. Ha nem a várt státuszon áll, megáll és visszairányít. Ez megakadályozza, hogy egy befejezetlen fázis alapján haladjon tovább. **A másik úton is így van** (QF4): a `quick-flow` 2. fázisa a `spec.md`, a 3. a `tasks.md` státusz-mezőjét olvassa belépő kapuként — a beszélgetésben elhangzott „igen" nem éli túl a `/clear`-t, a commitolt státusz igen.

**2. Minimális kontextus betöltés**
Az agent csak a szükséges fájlokat olvassa be, csak a releváns részeit. Ha mélyebb kutatásra van szükség, azt delegálja, és az eredményt összefoglalva hozza vissza — ne töltse tele a fő munkamemóriát. A cél: a fő kontextusablak ne telítődjön felesleges információval.

**3. Egyetlen kérdés egyszerre**
Ha a promptnak döntési ponthoz van szüksége a felhasználó inputjára, egyszerre csak egy kérdést tesz fel, megvárja a választ, majd iterál. Ez megelőzi az egyszerre több nyitott kérdés kavalkádját.

**4. Scope fegyelem**
Minden fázis szigorúan a saját scope-ján belül marad:
- Spec: csak az üzleti viselkedést írja le — nem tervez implementációt
- Plan: csak a spec scope-ját fedi le — nem bővíti, nem szűkíti
- Tasks: csak a plan tervezett módosításait bontja le — nem ad hozzá újat

**5. Státuszkezelés**
Minden fázis dokumentuma explicit státuszmezőt kap (`Piszkozat` → fázisspecifikus zárolt státusz). A következő prompt ezt olvassa be kapuként. A státusz-értékek **nem literálok** a promptban, hanem `<status:…>` tokenek (`status-keys.json`), és **nem rendezettek**: ahol egy kapu több állapotot fogad el, azokat **tételesen** kell felsorolni, „legalább ilyen" összehasonlítás nincs. A `quick-flow` mindkét artefaktuma is státusz-mezőt hordoz (QF2) — enélkül a `bs-cycle-status` minden fázisra „még nem futott"-at írna.

**6. Minőségellenőrzési lista (lezárás előtt)**
Minden prompt tartalmaz egy kötelező ellenőrzési listát, amelyet a fázis lezárása előtt le kell futtatni. Ez biztosítja, hogy a következő fázis egy teljes és konzisztens dokumentumot kap.

**7. TDD jelölés (04 + 06)**
A 04-es prompt tasks listát ír, a 06-os (implement) végrehajtja. A TDD ciklus a tasks listában explicit jelöléssel van rögzítve:

- `[RED]` — az agent először megírja a tesztet, amely **bukni fog**, mert az implementáció még nem létezik. Ez rögzíti a várható viselkedést kód formájában, mielőtt bármit implementálnánk.
- `[GREEN]` — ezután jön az implementáció, amelynek célja kizárólag az, hogy a `[RED]` teszt átmenjen. A `[RED]` task mindig megelőzi a párját.
- `[CHECK]` — minden logikai csoport végén kötelező: konkrét parancsot tartalmaz (pl. `npm test`, `npm run typecheck`), amelyet az agent ténylegesen lefuttat. Egy `[RED]`/`[GREEN]` task nem számít késznek, amíg a csoportzáró `[CHECK]` nem zöld.

Nem minden task TDD: konfigurációs fájlok, docker, README, infrastruktúra-változások esetén nincs `[RED]`/`[GREEN]` jelölés.

**7/b. Csonkítás-mentes átemelés (KX2 a 02-ben, KX3 a 03-ban)**
A fázisok közötti átadás **kétirányú hibát** tud véteni, és mindkettőre van szabály. (a) A bemenet **túl absztrakt** (hivatkozik valamire ahelyett, hogy tartalmazná) → `Hivatkozás-feloldás`: a hivatkozást fel kell oldani a forrásból, literál értékekkel. (b) A bemenet **már kidolgozott** (OpenAPI-leíró, teljes payload, hibamátrix, többlépéses teszt-forgatókönyv) → **szó szerint, csonkítás nélkül** kell átvinni: a 02-ben ez a `KX2` („ne zanzásítsd a teszteseteket"), a 03-ban a `KX3`. Az irány mindkettőben: **bővítés és pontosítás igen, összevonás és elhagyás nem.** A `KX3` explicit feloldja azt a három szabályt, ami korábban az egyszerűsítés felé nyomta a plan-írót („a plan terv, nem archívum" — az a repó forrásfájljaira szól; „az absztrakciós szintet fel kell oldani, nem reprodukálni" — a *szintre* igaz, a *tartalomra* nem; a 05 duplikáció-kategóriája — az nem vonatkozik a kötelező önhordóságra). A 05 mechanikus kapuja méri (`V1`: a spec szerződés-blokkjai megvannak-e a plan-ben; `V2`: a teszt-szekciók terjedelme), a prózai csonkítás pedig az `analyzer` 3. kategóriájában marad. **Prompt-módosításnál ezt ne rontsd el:** a „tömörítsd", „ne duplikálj", „a plan legyen rövid" jellegű utasítás ebben a fázisban adatvesztést okoz.

**7/c. Útvonal-konvenció (RP1)**
Egyetlen közös blokk (`prompts/shared-hu/path-format.md`) definiálja: **kód- és fájl-hivatkozás a repó gyökeréhez** képest relatív (a parancsok ott futnak, és a `05` mechanikus kapuja oda oldja fel a horgonyokat), **dokumentum-link a fájl saját könyvtárához** képest (hogy kattintható legyen); abszolút, gép-specifikus és `file://` alak tilos a dokumentum tartalmában (a chat-válaszban adott kattintható link kivétel). A kapu `R1` checkje méri. **Prompt-módosításnál ne írd újra fázisonként** — a szabály korábban három helyen, egymással ütköző tartalommal élt.

**7/d. A kapu-konfiguráció együtt mozog a struktúrával (GC1)**
Több determinisztikus kapu a projekt `conventions.md`-jéből olvas (TR3 riport-artefaktumok és útvonal-alap, Sonar küszöbök, teszt-parancsok, portok, merge-stratégia). Ha egy ciklus olyat változtat, amit egy kapu ott keres, a `conventions.md` frissítése **a ciklus része**: explicit döntés → a plan tervezi konkrét tartalommal → `[GREEN]` task → a kapu ugyanebben a ciklusban újra fut. A `00`-ra csak akkor megy vissza, ha magát a **projekt-konvenciót** kérdőjelezzük meg. Határvonal (TC1/c): riport-artefaktum/útvonal-alap/riport-parancs → `conventions.md`; teszt-recept és koordináta → `specs/test-conventions.md`. **Séma-váltásnál kötelező migrációs őr:** ha egy struktúra jelentése változik, de a formátuma nem (mint a TR5-nél: a tábla utolsó oszlopa `test-report/` gyökér → kör-mappa), a régi adat **csendben félreértelmeződik** — ezért a szekcióba verzió/szemantika-jelölő kell (`**Artefaktum-útvonal alapja:**`), és a kapu a jelölő hiányában nem találgat, hanem `exit 2`-vel megáll a pótlandó sorral. **Prompt-módosításnál:** ha egy kapu által olvasott struktúra jelentését módosítod, mindig tedd mellé a jelölőt és a migrációs ágat.

**7/e. Egy fogalom, egy útvonal-alak (TR5/c) + a riport-fázis (TR6)**
A kör-/fázis-mappának **három bázisa** van: `run-tests.py --round-dir` (repó gyökér), `report-gate-check.py --report-subdir` (ciklus-mappa) és a projekt riport-parancsainak `<phase-dir>` / `REPORT_PHASE_DIR` alakja (`test-report/`). Az egyik alak beragasztása egy másik bázist váró paraméterbe **nem hibaüzenetet ad, hanem rekurzív riport-fát** (`test-report/test-report/…`, `test-report/specs/…`). Ezért: a `07` **0/a** szekciója egy táblában definiálja mind a hármat, a `test-report/` felső szintje **zárt lista** (idegen mappa = útvonal-hiba, törlendő — a takarítási tilalom csak a `round-NN/`-re szól), a szkriptek mind a három alakot normalizálják, a `plan.md` gépi táblája két nem felcserélhető helyőrzőt kap (`{round}` teljes útvonal / `{phase}` fázis-mappa), a `run-tests.py` a futtatás **előtt** `exit 3`-mal megfogja a dupla prefixet, a `report-gate-check.py` pedig layout-őrrel `exit 1`-gyel bukatja. **A TR3 kapu kizárólag a `## Teszt-riportolás` tábla SORAIT kéri számon** — a szekció prózáját nem: ezért az alkalmazás-oldali bizonyíték (REST kérés/válasz audit-napló) is táblasor. A `**Riport-fázisok:**` mező (TR6) dönti el, hogy a `06` csak a `check-log.md`-t írja-e, vagy a záró állapotról a teljes riport-készletet is. **Prompt-módosításnál:** ne írj ki fázisonként útvonalat magyarázat nélkül, és ne vezess be negyedik bázist.

**7/f. A teszt-forgatókönyv nem próza (TS1–TS6)**
A `03` hajlamos a spec teszteseteit „nagy vonalakban" átvenni (típus + érintett fájl, lépés és elvárt eredmény nélkül) — akkor is, ha a felhasználó a spec-ben lépésről lépésre leírta. A prompt-oldali szabályok (TC1/a, KX3, a `quality-check-plan` „lépésenkénti híváslánc" pontja) mind megvoltak, de **egyik sem volt ellenőrizhető**: a `<sec:testing_strategy>` szabad próza, a `V2` kapu pedig **aggregált** sorszámot mér, amit a gépi tábla és a bootstrapping hossza elfed. Ezért a plan kötelező, **per-teszteset szerkezetet** kapott: a `### <sec:plan_test_scenarios>` szekció `TS-NN` blokkjai (`Mit tesztelünk` · `Előfeltétel` · négyoszlopos lépés-tábla · `Takarítás`), az `analyze-gate-check.py` hat checkjével (TS1 létezés · TS2 teljesség · TS3 **lépésenkénti** konkrétum · TS4 placeholder-tilalom · TS5 kétirányú `DoD-NN` ↔ `TS-NN` · TS6 hézagmentes azonosítók). A TS3 kemény padlója: az elvárt eredmény cellában legalább egy backtickes érték vagy szám — a „sikeresen lefut" nem eldönthető. A forma azonos a `bs-manual-test-plan` `TG-NN` csoportjaival: a `TS-NN` ugyanaz, a keletkezés helyére felhozva, ezért az MT innentől tényleg **összeszerel**. **Prompt-módosításnál:** a „tömörítsd a teszt-szekciót" jellegű utasítás itt adatvesztés, és minden új prózai teszt-elvárás mellé kérdezd meg, mivel lesz gépiesen mérhető.

**7/g. Egy zöld teszt nem bizonyítja, HOL volt zöld (EV1–EV5)**
Egy ciklus a dev környezetre telepített, a tesztjei viszont lokális célpontra futottak — egy `…:dev-e2e` **nevű** script configjában `baseURL: "http://127.0.0.1:5178"` állt. Minden zöld lett, a validálás PASS-ra zárt, és nem derült ki, hogy a telepített komponens el sem indult. A plan **nem** volt felületes: a `<sec:environment_coords>` végig dev URL-eket sorolt. A baj az volt, hogy **a teszt tényleges célpontja sehol nem volt látható** — parancs helyett egy npm-script neve, a cím egy konfigfájlban —, a bizonyíték (JUnit XML, Allure) pedig nem rögzíti a megszólított hostot. Ezért: kötelező `**<field:f_target_env>:**` mező a planben (EV1), kötelező `<field:f_environment>` oszlop a futtatási táblában (EV2), és nem-lokális kategóriánál a cél-host **literálisan a parancsban** (EV3) + **elérhetőségi probe** ugyanoda az `Előfeltétel` cellában (EV4) + `localhost`-tilalom (EV5, a `TS-NN` hívásokra is). A `run-tests.py` a futtatás **előtt** megáll (`exit 4`), és a környezetet a bizonyítékba írja. **Prompt-módosításnál:** minden új teszt-futtatási útnál kérdezd meg, honnan olvasható ki a CÉLPONT — ha csak egy név utal rá, az nem elég.

**7/h. A konzerváló szabály nem hoz létre tesztet (TD0–TD7)**
A `TS1–TS6` után a panasz nem szűnt meg, csak áthelyeződött: formailag hibátlan `TS-NN` blokkok, bennük egyetlen kérés-válasz pár. Az ok, hogy a keret minden teszt-szabálya **konzerváló** volt (`KX2`/`KX3` megőrzi, amit a bemenet hordoz; a `TS3` **kemény padlót** ad) — egy gyenge modell pedig pontosan a padlóra optimalizál, egy mondatos bemenetből meg nincs mit megőrizni. A hiányzó lépés a **létrehozás**: a `prompts/shared-hu/test-scenario-design.md` blokk (a `03` skill és a `plan-fixer` beemeli) kitöltendő kérdésekké alakítja a teszt-tervezést — `TD1` dimenzió-leltár (a szorzat kiírásával dönti el, **hány** forgatókönyv kell), `TD2` megfigyelési négyes (válasz · **megszámolt** mellékhatás · **közvetlenül kiolvasott** állapot · **negatív kontroll**), `TD3` megszámolhatóság (a számlálás forrását meg kell nevezni), `TD4` negatív kontroll az izolációhoz, `TD5` kitöltött kalibrációs minta (a **sűrűséget** kell másolni, nem a témát), `TD6` önteszt. A `TD0` hatókör-jelölés tartja a spec/plan határvonalat: a spec-fázisban viselkedés-szint és parancs-tilalom, a plan-fázisban literál értékek. **Prompt-módosításnál:** minden új teszt-elvárásnál kérdezd meg, hogy a szabály **megőriz** vagy **létrehoz** — és ha a bemenet lehet egyetlen mondat is, akkor kell mellé generáló recept. A padló önmagában nem termel részletet. A `TD6` pontjai szándékosan **jelöltek** egy későbbi determinisztikus checkhez, nem kapu: kapu-fogat csak akkor fizess, ha egy valódi ciklus megmutatja, hogy a recept nem elég.

**7/i. A kötelező szekció csak akkor kötelező, ha a HIÁNYA is mérhető — és a terv a célt is megmondja (TS7 · TA1 · WY1)**
Egy valódi ciklus (cycle-30) két rést mutatott meg. (a) A `03` a spec teszt-szekciójának **saját címsor-szerkezetét** hozta át (`Teszteset 0…7`, alatta „REST szekvencia"/„Verifikáció" felsorolás), a kötelező `<sec:plan_test_scenarios>` szekciót pedig **meg sem nyitotta** — így a `TS1–TS6` checkeknek nem volt mit mérniük, a plan mégis „tele volt teszttel". A szerkezet-másolás **a mérés vakfoltja**: a hiányzó szekció csak akkor derül ki, ha a hiányát egy másik, kitöltött struktúrán keresztül is számonkérjük. Ezért a `TS7`: a `<sec:spec_coverage>` tábla **minden sora megnevez legalább egy `TS-NN`-t** (vagy az indoklást, hogy az eset nem tesztelhető) — a spec teszteseteit **konvertálni** kell forgatókönyvvé, nem prózaként átmásolni. (b) A teszt-leírás akkor sem volt egy embernek végrehajtható, ha a lépések megvoltak: hiányzott, hogy az új tesztfájl **milyen kerettel** készül, **milyen paranccsal futtatható önmagában**, milyen **fixture/mock/tesztadat** kell hozzá (és hogy az új fájlként szerepel-e a `<sec:planned_changes>`-ban), és melyik **teszt-függvény** melyik esetet fedi — ez a `TA1` adatlap. (c) A `<sec:planned_changes>` bejegyzések a MIT írták le, a MIÉRT-et nem: a felhasználó kézzel írta bele minden bejegyzésbe. Ezért a `WY1`: minden `[P-…]` visel `<field:f_purpose>` sort (a változás UTÁNI viselkedés + a megszüntetett baj + a spec-forrás `DoD-NN`), különben nem eldönthető, mikor van kész a változtatás, és hogy egy eltérő megoldás jó-e. (d) Végül a teszteseteknél is ugyanaz hiányzott, mint a tervezett módosításoknál: a **cél**. A `TD7` ezért minden teszteset elé — `TS-NN` blokk, unit-táblasor, integrációs/E2E flow, tesztfájl-adatlap — kötelezővé teszi a „mit ellenőriz és miért” mondatot, **állításként**, a `DoD-NN`-nel; a cím megismétlése nem cél. Enélkül egy bukott tesztről a `07`-ben nem eldönthető, hogy a kód romlott el vagy a teszt rossz — a fixer pedig a legkönnyebb zöldítő utat választja. Mérése: a `TS2` tartalmi padlója a `<field:f_what_we_test>` soron, a `TA1` kötelező `<field:f_what_it_checks>` sora és a teszteset-tábla kötelező oszlopa. Mindhárom a `03` promptjában **kalibrációs mintát** is kapott (kitöltött bejegyzés, kitöltött adatlap) — a 7/h tanulsága szerint a padló önmagában nem termel részletet. (e) Két kiegészítés ugyanebből a ciklusból: a `TS8` szerint minden REST-lépést hordozó `TS-NN` blokk mellé kell egy ```http kódblokk is (a lépés-tábla cellája a **gépnek** szól, a `.http` az **embernek** — a `bs-manual-test-plan` MG9-e ugyanezt már megkövetelte, csak egy fázissal később); a `PH1` pedig a gépi futtatási tábla új `<field:f_phase>` oszlopa (`implement` / `validate` / `mindkettő`, **üres cella = mindkettő**), amivel a `run-tests.py` `--phase` szűrője dönti el, mit futtat a `06` a fázis végén és mit a `07`. A `PH1`-nél a tervezési elv: **a hallgatás soha ne jelentsen kihagyást** — egy üresen maradt cellától nem tűnhet el teszt —, és mivel a `dod-check.py` a validálási kör bizonyítékaiból joinol, a kapu bukik, ha egyetlen kategória sem fut `validate`-ben. **Prompt-módosításnál:** ha egy szabály egy kötelező szekcióra hivatkozik, mindig kérdezd meg, mi történik, ha a fázis a szekciót **létre sem hozza**, hanem másképp nevezi el — és mérj egy olyan struktúrán keresztül, ami akkor is létezik. Új opcionális jelölésnél pedig mindig a **biztonságos irány legyen az alapértelmezés**.

**7/j. A státusz-mező önbevallás — a FOGADÓ fázis ellenőrizzen (EG1 · GS2 · TT1 · T6)**
Egy éles ciklusban a `plan.md` `<status:ready_for_tasks>` státusszal állt, miközben a mechanikus kapu hét blokkoló megállapítást adott rá (nincs `<sec:plan_test_scenarios>`, nincs `<sec:machine_run_table>`, nincs `<sec:spec_coverage>`). A `04` a státuszt elhitte, és egy olyan planből írt tasks listát, amelyből a tesztek **nem bonthatók le**. A tanulság nem az, hogy több szabály kell a `03`-ba — a szabályok megvoltak, és a kapu is megfogta volna —, hanem hogy **a lezáró fázisnak nincs érdeke megbukni a saját kapuján**; a fogadónak viszont van, mert a hiányos bemenetből ő ír rossz listát. Ezért a `04` belépő kapuja **lefuttatja** az `analyze-gate-check.py --plan-only`-t (`EG1`), nem csak elolvassa a státusz-mezőt; a `03` pedig a lezárás nyomát a `plan.md` fejlécébe (`<field:f_gate>`) és a záró üzenetébe is kiírja (`GS2`, a `GA1` javaslat-check méri). Ugyanez a ciklus mutatta meg, hogy a lefedettségi lánc (`DoD-NN → [P-…] → task`) **a teszteket kihagyta**: a nyolc teszteset egyetlen `[RED]` taskba mosódott, a `[CHECK]`-ek pedig a teljes suite-ot futtatták — négyen ugyanabba a logfájlba, `>`-tal, tehát öt futásból egy bizonyíték maradt. Innen a `TT1` (kötelező `<sec:test_coverage>` tábla: minden `TS-NN` és minden futtatási kategória kap létrehozó és futtató taskot, vagy indokot) és a `T6` (két `[CHECK]` nem írhat `>`-tal ugyanabba a fájlba). Végül a hivatkozási lánc a tesztek szintjén is nevet kapott (`TI1`/`TI2`/`TX1`): `TS-NN` + `TC-NN` közös, ciklus-szintű névtér a planben, `— test [TC-01]` jelölés a task sorában, és **egy futtató `[CHECK]` egy teszt** — mert egy gyűjtő „futtasd a unit teszteket” sor mögül sem a lefutás, sem a bukás nem azonosítható. **Prompt-módosításnál:** ahol egy fázis egy másik fázis **saját magának írt** mezőjében bízik, ott kérdezd meg, mi ellenőrzi ezt gépiesen — és a lefedettségi láncnál mindig nézd meg, hogy a **tesztek** benne vannak-e, nem csak a terv-szekciók.

**7/k. Ha a második leszállítandó rendre gyengébb, a FÁZISHATÁRT kérdőjelezd meg, ne a szabálysűrűséget (03a + 03b)**
A negyedik teszt-keményítő kör nem szabály volt, hanem **fázishatár**. A `TS1–TS6`, a `TD0–TD7` és a `TS7`/`TA1`/`WY1` mind ugyanarra a hibára válaszolt — a teszt-terv a fázis végén, a legkevesebb figyelemnél készül —, és mindegyik ugyanott ért véget: a szabály megvolt, a kapu megfogta volna, a fázis mégis egy mondatos forgatókönyveket termelt. A `03` (1042 sor, a keret legnagyobb skillje, a `04` háromszorosa) ezért **két fázisra hasadt**: `03a` a kód-terv, `03b` a teszt-terv, **egyetlen `plan.md`-be**. A nyereség nem a token, hanem három szerkezeti tulajdonság: (a) egy fázis, amelynek a teszt-szekció az **egyetlen** leszállítandója, nem tudja „nem megnyitni" azt (a `TS7`-vakfolt megszűnik); (b) a `03b` **lezárt** kód-tervről indul, tehát a `TA1` adatlap és a `TS-NN` hívások literál értékei nem találgatások; (c) a `7/j` elve újra alkalmazható: a `03b` **fogadó** fázisként lefuttatja a kód-fél kapuját (`--plan-code-only`), mert neki érdeke a jó bemenet. A `04` felé menő kontraktus **nem gyengült**: a `03b` a teljes `--plan-only` kapuval zár, és a `04` EG1-je változatlan. **Prompt-módosításnál:** ha egy fázis kimenete két, egymástól minőségileg független artefaktum-halmaz, és a második rendre gyengébb, akkor először a **fázishatárt** kérdőjelezd meg, ne a szabálysűrűséget — és ha hasítasz, mindig kérdezd meg, melyik fél **ír** és melyik csak **olvas** egy megosztott dokumentumban (itt: a `03b` három, tételesen felsorolt bővítést tehet a kód-félbe, semmi mást).

**7/l. Egy zöld teszt azt sem bizonyítja, hogy bármit ELLENŐRIZTÜNK (CK1 · RED1 · TB1–TB3 · EV6 · TR7 · RV-FB1)**
A `7/g` kimondta, hogy egy zöld teszt nem bizonyítja, **hol** volt zöld. Egy éles ciklus (cycle-30) megmutatta, hogy a lánc egy lépéssel korábban is elszakad: **egy zöld teszt azt sem bizonyítja, hogy bármit ellenőriztünk, sőt azt sem, hogy a tervezett parancs futott le.** A tesztfájlba `assert True` vázak kerültek, a nyolc `[CHECK]` task helyett egyetlen, szűrő nélküli futás került a naplóba (annak `Task` cellájában intervallummal), három `[CHECK]` szelektor pedig már átnevezett függvényre hivatkozott — és mindez **zöld** volt: a `passed` számláló nőtt, a `dod-check.py` a teszt **nevére** joinolt, a `rest-logs` mappa korábbi körök fájljaitól telinek látszott, a review pedig **fallback ágon** futott, ahol a szempontlista fizikailag nem volt jelen. Hét determinisztikus kapu zárja: `CK1` (a `[CHECK]` szó szerint, egyenként, egy naplósor = egy task), `RED1` (minden `[RED]`-hez bukott futás — `RED-EXEMPT` indoklással felmenthető), `TB1` (vacuous teszt-törzs tilalma konzervatív mintakészlettel), `TB2` (a parancs szelektora létezik a tesztfájlban), `EV6` (nem-lokális kategóriánál a **körben keletkezett** bizonyíték tartalmazza a cél-hostot), `TR7` (a kör-mappa artefaktuma a kör alatt keletkezett), `RV-FB1` (a review-szempontlista build-time **mindkét** végrehajtási ágon jelen van). **Prompt-módosításnál három kérdés:** (a) ha egy fázis „kész" jelzése egy **számláló** vagy egy **név** egyezésén áll (`X passed`, teszt-név → `DoD-NN`), mi bizonyítja, hogy a számláló mögött **történt** is valami? (b) ha egy szabály a **tervre** vonatkozik (`TX1`: egy `[CHECK]` egy teszt), mi ellenőrzi, hogy a **végrehajtás** is így ment? (c) ha egy szabály egy **subagent** promptjában él, mi történik a **fallback** ágon, ahol az a prompt nem is fut? A prózai szabály itt szándékosan **másodlagos**: az implementálónak érdeke a pipa (`7/j`), az LLM-reviewer pedig nem kapu — a **szerkezeti** próza (a lista fizikai jelenléte mindkét ágon) viszont az.

**7/m. A zöld kör nem bizonyítja, hogy a KATEGÓRIA lefutott (RUN1 · TP4/b · EV7 · SK1)**
A `7/l` a **teszt** szintjén kérdezte meg, mi bizonyítja, hogy történt is valami. Egy éles ciklus megmutatta, hogy ugyanez a kérdés a **kategória** szintjén még nem volt feltéve: a plan gépi futtatási táblájában ott állt egy `dev` kategória, a tesztek dev-módja **meg sem íródott**, a kategória **soha nem futott le** — és a kör `PASS`-ra zárt. A validálási problémákat **két ágens is elemezte**, egyik sem vette észre: ez **hiány**-állítás („egy deklarált kategória nem futott le"), amit egy LLM-review szerkezetileg rosszul lát, determinisztikusan viszont két meglévő artefaktum triviális **joinja**. A lánc négy ponton szakadt: (a) a tábla **nem a keret sémáját** követte (`Recept | Kategória | … | Időkorlát | …`), ezért a fix oszlop-pozíciókkal olvasó `run-tests.py` nem hibaüzenetet adott, hanem **rossz cellákat használt** — a fázis **kézi parancsokra** esett vissza, ahol egyetlen `EV` kapu sem fut le; (b) semmi nem mérte, hogy a kör **lefedte-e** a tábla `validate`-fázisú sorait (a PASS-t adó körről egyáltalán nem született `results.json`); (c) a parancs `DEV_BASE_URL=…`-t állított, amit a tesztkód **nem olvasott** — a „dev" futás bájtra a lokális volt; (d) az egyetlen valódi teszt `pytest.skip`-pel lépett ki, a `dod-check.py` pedig a `<skipped>` esetet **`PASS`-ként** indexelte, tehát a `DoD` bizonyítékot kapott. Négy determinisztikus kapu zárja: `TP4/b` (a tábla oszlop-sémája kötelező, a `03b` lezárásakor — a parsert **szándékosan nem** tettük „okossá", mert az elrejtené a hibát), `RUN1` (TELJES körben minden `validate`-fázisú kategória szerepel a kör `results.json`-jában; `RUN-EXEMPT` sorral felmenthető), `EV7` (javaslat: a nem-lokális kategória env-változója megjelenik-e a futtatott tesztkódban), `SK1` (a `skipped` eset nem bizonyíték — sem a `dod-check.py` joinjában, sem a `07` kapujában; `SKIP-EXEMPT` sorral felmenthető). **Prompt-módosításnál két kérdés:** (a) ha egy fázis egy **gépi leírót** (tábla, manifest, konfiguráció) használ bemenetként, mi garantálja, hogy a leíró **parse-olható**, és mi történik, ha nem — hibaüzenet, vagy **néma visszaesés** kézi útra? (b) ha egy bizonyíték **három állapotú** lehet (pass / fail / skipped), a kapu mindhárommal számol-e, vagy a harmadikat **csendben a jóhoz sorolja**? Ugyanez a keresés fedett fel egy latens hibát is: a tábla fejlécét egy magyar szó-literál ismerte fel, tehát **angol** projekt-nyelven a fejlécsor adatsorként futott volna — a nyelvfüggő felismerés helyett **szerkezetből** (az elválasztó sort megelőző sor) kell dolgozni.

**7/n. A bizonyíték SZEMCSÉZETTSÉGE — egy konkrét teszt HOL futott (EV8 · EV9 · EV10 · RL1 · RL2)**
A `7/l` a **teszt**, a `7/m` a **kategória** szintjén kérdezte meg, mi bizonyítja, hogy történt is valami. Maradt egy harmadik szint: **egy konkrét teszt HOL futott.** A bizonyíték eddig kategória-szemcsés volt — a `results.json` a *deklarált* környezetet írta, a JUnit XML hostot nem rögzít, a `rest-logs` pedig egy **lapos halom** volt, amelyből nem derült ki, melyik teszt mit hívott (egy éles ciklus kör-mappájában 50 örökölt, `127.0.0.1`-es naplófájl állt: a mappa *telinek látszott*). Ezért egy remote környezetre szóló ciklusban **semmi nem kérdezett rá arra, hogy van-e egyáltalán remote teszt** — nyolc lokális forgatókönyv mellett is minden kapu zöld maradt. A feloldás **kettéválasztja a szándékot és a bizonyítékot**: a `TS-NN` fejléce `[local]`/`[remote]` címkét kap (szándék, a `03b` kapuja méri — `EV8` a címke meglétét, `EV9` a remote-lefedettséget, `EV10` a címke ↔ gépi tábla konzisztenciát), a REST-napló pedig teszt-szerinti almappába megy (`rest-logs/<local|remote>/<teszt>/`, bizonyíték, a `07` kapuja méri — `RL1` az útvonal ↔ tartalom viszonyt, `RL2` a címke ↔ bizonyíték joint). **Az érték a kettő JOINJÁBAN van:** egy `[remote]`-nak jelölt teszt, amelynek naplója `local/` alá került vagy hiányzik, **önellentmondás**. Két döntés tette működőképessé: a címke **nyelvfüggetlen literál**, mert útvonalra joinol (projekt-nyelvi címke mellé fordítási réteg kellene a kapuban ÉS a naplózó fixture-ben, és a kettő csendben szétcsúszna — ugyanaz a csapda, mint a `gyors`/`nehéz` a `--type` kapcsolónál, csak most **előre** elkerülve); a naplózó pedig a **teszt saját jelöléséből** sorol be, nem a hívott címből, mert a cím mindkét irányban téved (egy `oc port-forward` mögötti `127.0.0.1` **remote**, egy compose service-név **local**) — ebből következik, hogy az útvonal önmagában nem bizonyíték, a bizonyíték a mappa **tartalma**, a port-forward pedig **deklarált** (a `Környezetek és végpontok` tábla `remote` sorainak lokálisnak látszó címei). **Prompt-módosításnál két kérdés:** (a) ha egy bizonyíték **szemcsézettsége** durvább, mint a döntésé, amit alátámaszt (kategória-szintű bizonyíték egy teszt-szintű állításhoz), mi tölti ki a rést — vagy csak **reméljük**, hogy a durva bizonyíték a finom állításra is igaz? (b) ha egy szabály azt mondja, „ezt mindig **gondold** végig", mi bizonyítja, hogy végig lett gondolva — van-e olyan artefaktum, amiből a mulasztás **hiányként** kiolvasható?

**7/o. A MÁSIK ÚT is elcsúszhat — minden keményítő kört kérdezz vissza a quick-flow-ra (QF1–QF20 · QT1–QT6)**
A `7/f`–`7/n` mind a **teljes flow-t** keményítette, és mindegyik kör lezárásakor a kérdés az volt, hogy melyik fázis-skillbe kerül a szabály. Egyik kör sem kérdezte meg, hogy **igaz-e ugyanez a másik úton** — a `bs-quick-flow` egyetlen `inprove-list` hatókörében sem szerepelt, a fenti fájl-tábla pedig **nem is sorolta**, tehát minden felülvizsgálat átlépett rajta. A mérés nem vélemény volt, hanem három mérhető törés: (a) a `bs-cycle-status` **minden** quick-flow ciklusra `MÉG NEM FUTOTT`-at írt, mert a lightweight ág `tasks.md`-t és státusz-mezőt keresett, a flow viszont `task.md`-t írt, státusz nélkül — vagyis a jóváhagyás ténye **sehol nem volt perzisztens**, `/clear` után az ágens nem tudta, jóvá volt-e hagyva a spec; (b) három szekció tényszerűen elavult volt (`sdd-skill` telepítési útmutató, nem létező `/goal` parancs, bedrótozott `master` ág és Jira-prefixes commit, miközben a nagy flow mindezt a `conventions.md`-ből olvassa) — egy telepített projektben a két flow **más ágnévvel és más commit-formátummal** dolgozott; (c) a három opcionális ágens kontraktusa elcsúszott (a `reviewer` fájlt ír egy nem létező mappába, `plan.md` nélkül; a `researcher` `called_by` listája nem is tartalmazta a quick-flow-t). A feloldás **nem** a nagy flow gépezetének átemelése volt (`D6`: a kapu-scriptek, a `DoD-NN → [P-…] → task` lánc, az önjavító hurkok és a doc-sync **maradnak** a teljes flow-ban — ez adja a két út közti különbséget), hanem a **tanulságoké**: státusz-mező + fázis-kapu mindkét artefaktumban (`QF2`/`QF4`), a `conventions.md` git-szekciójának olvasása (`QF1`), a közös shared-blokkok beemelése (`QF10`/`QF13`/`QF14`/`QF15` — RP1, KX2, AV1, GC1), a kimondott IM1-ellenpár (`QF9`), a flow-határ négy rése (`QF5`–`QF8`: BQ2 ciklusszám, roadmap, `docs-generated/` drift, `manual-test-plan` második belépő) és a teszt-keményítés **közepes** csomagja (`QT1`–`QT6`: cél-környezet, `[local]`/`[remote]` címke, „mit ellenőriz és miért", vacuous-teszt, `skipped` ≠ bizonyíték, egy futtatás = egy azonosítható teszt) — kapu-script nélkül, mert a flow olcsósága a lényege. **Prompt-módosításnál három kérdés:** (a) ha egy kör tanulságot hoz a teljes flow-ban, **igaz-e ugyanez a quick-flow-ra**, és ha igen, átvihető-e **kapu nélkül, prózában**? (b) ha egy segédparancs vagy script **két** flow-t szolgál ki, mindkét ágát megnézted-e — vagy csak azt, amelyiket éppen módosítod? (c) ha egy fájl **nincs benne ebben a táblában**, az nem azt jelenti, hogy rendben van, hanem azt, hogy **nem nézte meg senki** — ezért került most ide a `quick-flow`, a `cycle-status` és az `export-doc` is.

**8. Megállási szabályok — és a 06 folytonossága (IM1)**
A 04-es (tasks) és 06-os (implement) prompt explicit felsorolja azokat az eseteket, amikor az agent megáll és visszakérdez — ne folytassa bizonytalan vagy ellentmondásos helyzetben.

A 06-ban ehhez **kimondott ellenpár** is tartozik: a fázis **egy futásban** dolgozza fel a task listát, egy task lezárása (pipa + `check-log` + task-commit) **nem** fázis-vég, és a megállás öt eseténél (Megállási szabály · fejezet gépi előfeltétele · explicit infrastruktúra-igény · 3-próba · minden task kész) **nincs több**. Ez azért kell kimondva, mert a keretrendszer többi fázisában a „*A válasz végén helyezd el a … kattintható linkjét*" mondat **megállás-jelző**; amíg ez a mondat a 06 per-task lépésében szerepelt, az ágens minden task után visszaadta a szót. **Prompt-módosításnál ezt ne rontsd el:** taskonkénti felhasználói riport, per-task link vagy „folytathatom?" kérdés nem kerülhet vissza a hurokba. **Ugyanez a másik úton is ki van mondva** (QF9): a `quick-flow` egy záró szekcióban sorolja fel a megállási eseteit, és utána tételesen kimondja, hogy ezeken kívül nincs megállás — a 3. fázis a `tasks.md` utolsó pipájáig egy futásban megy.

**9. Kereszt-fázisos konzisztencia ellenőrzés + önjavító hurok (05 — analyze)**
A 04 (tasks) után, az implementáció előtt egy analyze fázis fut, **három rétegben**:
1. **mechanikus kapu** (`analyze-gate-check.py`) — minden kör előtt: a gépiesen eldönthető ellenőrzések (hivatkozás-egyeztetés mindkét irányban, markerek, `⟂`, `DoD-NN`, kötelező táblák, futtatott artefaktumok, plan-horgonyok, `Konfiguráció-életút` üres cellái, task-határon átnyúló shell-változó, artefaktum-hang kemény padlója). A kapu **generálja** a riport két lefedettségi tábláját (`DoD-NN → [P-…] → task` lánc), és **leltárt** ad az ítélet-igényes checkek jelöltjeiről;
2. **`analyzer` × 3 hatókör** (read-only, egymással párhuzamosan) — a szemantikai kategóriák a `spec.md` ↔ `plan.md` ↔ `tasks.md` ↔ `conventions.md` négyesen, három körre osztva (`s1-dup-underspec` = duplikáció + alulspecifikáció, `s2-coverage` = ambiguitás + a lefedettség **tartalmi** ítélete a generált mátrixon, `s3-conventions` = konvenció-ütközés); minden kör a kapu `--emit-slices` módjával kimetszett **saját szeletéből** dolgozik, nem a teljes négyesből (SH1);
3. **`analyzer-exec`** (read-only, az előzőekkel **párhuzamosan**) — végrehajthatóság és artefaktum-tulajdon a `plan.md` + `tasks.md` + leltár hármasból.

Iterációnként **egy** teljes analyzer-kör fut (a `PASS` csak teljes körből adható), és négy rövidítő ág védi az iteráció-számot: a fixer **visszatérés előtt maga futtatja a kaput** (GS1), az orchestrátor kapu-futása utána védőháló — mechanikus regresszió esetén az visszamegy ugyanahhoz a fixerhez, analyzer-kör nélkül (G) —, ha a fixer semmit nem változtatott, a hurok megáll és kérdez (N), és ha minden `Must Fix` lokális, a fixerek egyetlen üzenetben, párhuzamosan indulnak (LF1). Ugyanez a kapu a `03` és `04` fázis **lezárása előtt** is lefut (M), hogy a gépies hibák a keletkezésük helyén derüljenek ki. FAIL esetén az 05 **orchestrátorként önjavító hurkot vezényel**: a legkorábbi érintett célfázishoz indít egy fixer-subagentet (a 02/03/04 fix-módja), downstream re-deriválás (`02→03→04`, célzott reconciliation) után újra-analyze fut — amíg PASS, vagy `max X=3` iterációig. A döntést igénylő kérdéseket a fixerek a `*-questions.md`-be gyűjtik (nem kérdeznek közvetlenül), és az orchestrátor teszi fel a felhasználónak (`FÁZIS/Knn` fejléccel); nyitott kérdésnél a hurok megáll, válasz után folytatódik. A hurok alatt a dokumentumok `[analyze-loop]` státusz-markert viselnek (auto-státusz + megszakítás-biztos folytatás), és csak a hurok végén készül egyetlen commit.

**Triázs-megállás (TR1) — a felhasználó dönti el, mit javítunk.** A hurok **minden** diagnoszta-kör után (az elsőt is beleértve) megáll, és **egyetlen** kérdésben — ez az egyetlen hely, ahol a keret egyszerre több döntést kér — a felhasználó kiválasztja, mely **eddig el nem döntött** `<status:must_fix>` tételeket javítsuk. A jóváhagyottak az **`analyze/analyze-task.md`** javítási listára kerülnek (a fixerek **kizárólag** ezen dolgoznak), az elvetettek `elvetve (triázs)` állapottal a riportban maradnak — audit-nyomnak — és **nem blokkolják a `PASS`-t**. Már eldöntött tételt soha nem kérdezel újra; a tisztán mechanikus (kapu-)tételek kérdés nélkül kerülnek a listára. Ha az első triázsnál a válasz `egyik sem`, hurok nem indul, a fázis `PASS`-szal zárul — a fázis-záró commit viszont **ugyanúgy kötelező**.

**Az analízis mappája (AD1) és a két artefaktum szerepe.** Minden analízis-fájl a `specs/cycle-NN-<cycle-name>/analyze/` almappában él: `analyze-report.md` (**diagnózis és audit-nyom** — mi a baj, miért blokkol, hogyan lenne helyes, mi lett vele), `analyze-task.md` (**munkalista** — mit csinálunk, hol tart) és `slices/` (a kapu `--emit-slices` kimenete). A kettőt ugyanaz az `AF-NN` / `AC-NN` / `AN-NN` / `AX-NN` azonosító köti össze; **az azonosítót sehol nem szabad átírni**, mert a túlélés-szabály (TS) szó szerinti egyezésre épül: ha egy tétel több egymást követő iterációban `NEM oldódott meg`-ként jön vissza, a hurok **per-item eszkalál** (kérdés a felhasználónak) ahelyett, hogy a globális `max X`-ig vakon égetné a köröket. Megszakadás után az `analyze-task.md` a **legerősebb horgony**: ami rajta van, azt a felhasználó már jóváhagyta — kérdés nélkül folytatható.

**10. Önjavító hurok a kód-fázisban (07 — validate + review)**
A 05-analyze mintáját a **07-validate** veszi át, egyetlen hurokban a tesztekre ÉS a kódreview-ra (RV1). A teljes kör négy lépése (VD13 — olcsó → statikus → drága): gyors tesztek → **statikus réteg: Sonar + kódreview** (`reviewer` subagent, csak ha a gyors tesztek zöldek) → nehéz tesztek + regresszió → DoD/tasks/riport-kapu. A statikus réteg azért előzi meg a nehéz teszteket, mert a Sonar és a review stack nélkül fut, a findingjaik javítása viszont megváltoztatja a kódot — fordított sorrendben minden statikus finding ára egy eldobott E2E-futás lenne. Bármelyik bukása a kör FAIL-je, egyetlen naplóbejegyzéssel a `validation-report.md` `# Validation History`-jába: teszt-/Sonar-/DoD-hibára az `implement-fixer` (`## Validációs javítások`), review-findingra a `review-fixer` (`## Review javítások`) indul — mindkettő 06 fix-mód, közös `[validate-loop]` markerrel és **közös** leállási korlátokkal (per-item 3 egymást követő / 5 összes, plusz 5 egymást követő FAIL-futás). VD3 anti-„teszt-csalás" + VD3a szerződés-integritás kapu, VD5 eszkaláció 03/02-re. A javítás után könnyű kör, majd kötelező **teljes megerősítő kör** — PASS csak teljes körből, tiszta review-val. A review a 07-be költözésével a korábbi 09-es „re-validate" ág megszűnt (az a 07 gépezetét duplikálta); a `09-merge` így már csak a **kézi megerősítéssel** záruló beolvasztás (RD8). A hurkok közös konvencióit a `README-HU.md` „Önjavító hurkok" szekciója rögzíti.

**A diagnoszta subagentek megszakadás-tűrése (RV-INC).** A `reviewer` a jelentését **inkrementálisan** írja, nem egyetlen záró művelettel: ha a `test-report/code-review.md` fejlécében `<field:f_status>` = `<status:in_progress>` áll, a benne lévő findingok **valósak, csak hiányosak** — nem szabad eldobni vagy felülírni őket, és a review-kapu **sem zöldre, sem FAIL-re nem zárható** velük (a `validate-gate-check.py` ezt gépileg is elbukja). Ugyanez a megfontolás vonatkozik a `test-runner` és az `analyzer` megszakadt futására is: a részleges artefaktumot előbb megnézed, és a következő kör bemenetébe viszed tovább.

**11. Élő működési dokumentáció (08 — doc-sync)**
A 07-validate és a 09-merge közé egy dedikált **doc-sync** fázis ékelődik, amely ciklusról ciklusra naprakészen tartja a generált projekt-dokumentációt egy `docs-generated/` mappában (`system-overview.md` as-built működésleírás, `architecture.md`, részletes `CHANGELOG.md`, `design-drift.md`, mappa-index README + komponens README-k). Ez **nem** a négy önjavító hurok mintáját követi: a működése **„terv előbb, aztán mechanikus végrehajtás"** (a `doc-sync-planner` read-only subagent → `doc-sync-plan.md` pipálható terv → a fő ágens mechanikusan végrehajtja), majd egy **objektív, projektfüggetlen konzisztencia-kapu** (DS22: megszűnt/átnevezett azonosító `grep`, ábra-átkerülés, mappa-index halmaz-egyezés, coverage-marker) zár. Kapu-bukásnál **ember-vezérelt** javítás indul a `doc-sync-questions.md`-n keresztül (nem subagent-önjavító hurok). A `02-write-spec` „pull"-ként beolvassa a `system-overview.md`-t current-truth kiindulásként; a doc-sync „push"-ként írja — a kettő tartja őszintén a doksit. A 08-doc-sync és a 07 review-kapuja **független minőségi kapuk**: a reviewer csak kódra ad findingot, a generált doksik helyességét a doc-sync saját kapuja garantálja.

**12. Fázis-záró commit és fázishatár (PE1)**
Egy fázis **nem attól kész, hogy a státusz átáll, hanem attól, hogy a státuszváltás COMMITOLVA van.** A felhasználói megerősítés után a státuszírás és a commit **egyetlen, megszakíthatatlan lépéspár**: a kettő között nincs kérdés, nincs várakozás, nincs más munka. A commit után determinisztikus ellenőrzés jön (`git log -1 --oneline` + **üres** `git status --short` a ciklus mappájára), nem „érzésre". A közös eljárás egy helyen él (`shared-{hu,en}/phase-commit.md`), és build-time inline-olódik a `02`/`03a`/`03b`/`04`/`05`/`07` skillekbe (a `01`, `06`, `08` a saját záró szekciójában hordozza ugyanezt). A hurok-fázisokban (`05`, `07`) a hurok **alatt** nincs köztes commit, a fázis-záró commit a hurok lezárásakor **egyszer** történik — de **minden lezáró ágon kötelező**, kivétel nélkül (PASS, `max X` feladás, eszkaláció, Quality Gate-bukás).

**PE1 — a fázis a commit azonosítójának kiírásával VÉGET ÉR.** Ugyanabban a körben a következő fázisból semmit nem kezdesz el, a következő artefaktumot (`plan.md`, `tasks.md`, kód) **létre sem hozod** — „csak előkészítésként" sem. Ez a szabály **felülír** minden továbbmenetelre biztató jelet: a kontextus-összefoglalók/checkpointok korábbi teendő-listáit (azok a **múltat** rögzítik, nem parancsok a jelenre), a saját korábbi többfázisú tervedet és a felhasználó **korábbi** körben adott „menjünk végig a folyamaton" mondatát; csak a felhasználó **erre a körre szóló, explicit** kérése írja felül. **Prompt-módosításnál:** ha egy fázis záró üzenetébe új lépést teszel, kérdezd meg, hogy az a commit **elé** vagy **mögé** való — és hogy nem szivárog-e vele a következő fázis munkája ebbe a körbe. A commit üzenete a teljes flow-ban pontosan `cycle-NN: <fázis-tag>` (a `07`/`09` erre keres vissza); a `quick-flow`-ban ellenben a `conventions.md` git-szekciója dönti el (QF1) — a két formátumot ne mosd össze.

**13. Megszakadás-tűrés — a fázis a lemezről folytatható legyen**
A módszertan alapja a fázisonkénti `/clear`, tehát az ágens **rendszeresen kontextus nélkül indul újra** — és bármelyik fázis megszakadhat félúton. A keret ezért **minden** hosszú fázisban ugyanazt a négy horgonyt használja: (a) **státusz-mező** az artefaktumban (a jóváhagyás így nem a beszélgetésben marad, hanem commitolt tény — a `quick-flow` a `QF2`-vel épp ezt a rést zárta be); (b) **hurok-marker** a státusz mellett (`[analyze-loop]` / `[validate-loop]`) + naplózott kör-szám; (c) **munkalista-artefaktum**, amelyen a jóváhagyott, még el nem végzett munka áll (`analyze/analyze-task.md`, `tasks.md` pipái, `doc-sync-plan.md`); (d) **inkrementálisan írt** diagnoszta-jelentések (RV-INC) befejezetlenség-jelzővel. A fázisok eleje ezekből egy **döntési fát** futtat le, mielőtt bármit újrakezdene. **Prompt-módosításnál két kérdés:** (a) ha ez a lépés a végrehajtás közepén megszakad, mi a **lemezen** hagyott nyom, amiből a folytatás rekonstruálható — vagy csak a beszélgetésben él? (b) a folytatás **kérdez-e újra** olyat, amit a felhasználó már eldöntött (ez a leggyorsabb út a bizalom elvesztéséhez)?

---

## A promptok aktuális állapota

A rendszer aktívan használatban van: több tucat fejlesztési ciklus futott le ezekkel a promptokkal, és a fenti `7/*` tervezési elvek **mind egy-egy éles ciklus konkrét bukásából** származnak (a legutóbbiak a `cycle-28`–`cycle-30` körüli ciklusokból). A keményítő körök tételes terve és indoklása a `prompts/inprove-list*.md` fájlokban olvasható — ezek a **múltat rögzítik**, visszamenőleg nem írjuk át őket.

A skill fájlok a `prompts/skills-{hu,en}/`, a specialista ágensek a `prompts/agents-{hu,en}/`, a közös blokkok a `prompts/shared-{hu,en}/`, a projekt-nyelvi blokkok a `prompts/lang/{hu,en}/` mappában olvashatók.

> **Mielőtt bármit módosítanál, két olvasnivaló:** a repó gyökerében a `README-HU.md` (a felhasználónak szánt leírás, flow-ábrákkal és a hurkok konvencióival) és a `berki-spec-directory-structure.md` (mi hova települ a célprojektben, és melyik fájl kinek a tulajdona).

---

## Feladatod

Olvasd be a releváns skill fájlokat a `prompts/skills-hu/` mappából (és szükség szerint az ágenseket a `prompts/agents-hu/`-ból, a közös blokkokat a `prompts/shared-hu/`-ból) — a módosítást viszont **mindig a `-en` páron is** vezesd át —, majd segíts a következő fejlesztési célban:

Olvasd el a **Tervezési elvek** szekciót is (különösen a `7/*` köröket): mindegyik egy éles ciklus konkrét bukásából származik, és a végükön ott a *„Prompt-módosításnál…"* kérdés, amit az új szabályra is fel kell tenni.

**[IDE ÍRD LE A KONKRÉT FEJLESZTÉSI CÉLT — pl.:]**
- „A `03b` teszt-terv skill minőségellenőrzési listája hiányos — egészítsd ki."
- „A `06` implement skill nem kezeli megfelelően a delegálás esetét."
- „Vezess be egy új segédparancsot: changelog-generálás."
- „Általános felülvizsgálat: hol vannak következetlenségek a skillek között?"

Ha nincs konkrét cél megadva, végezz általános felülvizsgálatot: keress következetlenségeket, hiányzó megállási szabályokat, scope-szivárgást a fázisok között, és javasolj konkrét javításokat.

**Bármit is módosítasz, négy dolgot mindig ellenőrizz:**
1. **Mindkét prompt-nyelvi fán** átvezetted-e (`-hu` ÉS `-en`), és zöld-e a `lang-parity-check.py` (default + `--strict`) — agent-frontmatter változásnál a `sync-gemini-agents.py` is (előbb írás módban).
2. **Van-e script, amely a szabályt méri** — ha igen, a próza másodlagos; ha egy artefaktum-szerkezetet írsz át, **melyik script olvassa** (mert az némán rossz cellát fog használni, 7/m).
3. **Igaz-e ugyanez a MÁSIK ÚTRA** (`bs-quick-flow`) — és ha igen, átvihető-e kapu nélkül, prózában (7/o).
4. **Túléli-e a megszakadást** — mi marad a lemezen, ha a fázis félúton megáll (13. elv).
