<!--
  The PROJECT-LANGUAGE blocks of `05-analyze` (9.4 extraction).
  The installer inlines this file at build time in place of the INCLUDE markers,
  according to the chosen PROJECT language (`PROJECT_LANG`); the marker form is
  `lang/05-analyze.md#<anchor>`.
  The blocks were moved here VERBATIM — do not rephrase, do not unify.
  The ANCHOR lines are NOT part of the inlined text, they are delimiters only (8.9).
  CAUTION: no comment-closing sequence may appear in this leading note.
-->

<!-- ANCHOR:analyze-report-struktura -->
# Cycle NN: <title> — Analyze report

**Status:** PASS | FAIL
**Run:** YYYY-MM-DD HH:MM
**Loop:** <number of iterations> / <max X> (PASS | given up)
**Validated base:** `<name of the main branch>@<SHA>` · cycle branch: `<branch>@<SHA>` (BR1: `pulled in` | `was not needed`)

## Summary

_One or two sentences: is the set of four consistent, or where the problem is, and how the loop closed._

## Findings (last analyze)

### Must Fix
- [ ] <category> — <description> → target phase: <phase> (`file:location` if any)

### Suggestions
- <category> — <description>

## Executability inventory (category 6)

_Taken over from the output of the `analyzer` subagent; the fields marked `(gate)` come from the result of the mechanical gate. **Mandatory section** — if it is missing, the PASS cannot be accepted._

**Executed artifacts (gate, A1):** <in order / MISSING: ...>
**Tests promised in prose:** <promise → test case + task / MISSING>
**Artifact ownership:** <in order / present in the plan: ...>
**Status-updating task (gate, T3):** <none / Tnnn>
**Marker correctness (gate, T1/T2):** <in order / wrong [OPS]: ...>
**Destructive operations:** <approval + immutable identifier + rollback present / missing: ...>
**Anchor resolution (gate A2 + symbol judgement):** <resolvable / cannot be resolved: ...>
**Artifact voice (gate A3 + addressee judgement):** <in order / skill-voiced meta instruction left in: ...>

## Coverage matrix (requirement ↔ task)

_**Where it comes from (K/AG4):** this matrix is **generated by the mechanical gate** (`## Coverage matrix (generated)` block), and you append it here **verbatim** — the LLM does not derive it again. The `Covered (machine)` column means the **existence of the `DoD-NN` → `[P-…]` → task chain** and nothing else._
_**You make two corrections on the pasted table:**_
1. _if the `analyzer` reported a **content** gap on a `✓` row (`Affected DoD rows`), rewrite it to `✗`, and put the short reference of the `Must Fix` into the `Note` column;_
2. _the same, if the `analyzer-exec` signalled that the task of the row **will not run** (executability `Must Fix`)._

_**When (D12):** the final table goes into the report from the gate output of the **converging (last, `Must Fix`-free) round**, once. If the loop gives up at `max X`, paste the last available gate output, and mark it: "(state at the time of giving up)"._

| DoD | Plan section (`[P-…]`) | Task(s) | Covered | Note |
|---|---|---|---|---|
| `DoD-01` | `[P-CONFIG]` | T001, T002 | ✓ / ✗ | <reference of the content/executability Must Fix, if ✗> |

**Requirements beyond `DoD-NN`** (from category 5 of the `analyzer` — the generated matrix does not see these):
- <spec requirement without a task> _(or: "there is none")_

## Plan section ↔ task (PID1)

_EVERY `[P-…]` identifier of the plan appears; the "no task" row is valid with a justification. **This table is also generated by the mechanical gate** (`## Plan section ↔ task (generated — PID1)`) — append it here verbatim._

| Plan section (ID) | Referencing tasks | In order |
|---|---|---|
| `[P-CONFIG]` | T001, T002, T003 | ✓ |
| `[P-XYZ]` | — (justified by the `Plan coverage` table) | ✓ / ✗ |

_Deviations to be shown here: ID without a task · task pointing to a non-existent ID · reference by ordinal instead of `[P-…]` · executable plan section without an ID._

## Loop log

_Audit trail per iteration — the anchor for continuing after an interruption._

### Iteration 1
- **FAIL categories:** <categories>
- **Target phase:** <phase> (earliest affected)
- **Fix:** <summary of the fixer subagent in one line>
- **Open questions:** <PHASE/Qnn list or "none">
- **Re-derivation:** <which downstream phases were re-aligned>
- **Result:** PASS | FAIL → next iteration

### Iteration 2
...

<!-- ANCHOR:zaro-uzenet -->
   > *"The analysis found the design documents consistent. We can continue with step 6 (implement). Before starting the new phase, be sure to run a `/clear` command to empty the context, then use this command:*
   > ```
   > /bs-implement input: @specs/cycle-NN-<cycle-name>/tasks.md
   > ```"*
