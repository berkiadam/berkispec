# A telepítő által generált előzmény-fájl: a legutóbbi telepítés célmappája.
# Gépfüggő (lokális útvonalat tartalmaz), ezért nem verziókezeljük.
history

# Python bytecode
__pycache__/
*.pyc

# A /bs-run-tests cikluson kívüli, kényelmi teszt-futtatásának eredmény-fája (D7/KT7).
# Gépfüggő, újragenerálható, és SOHA nem ciklus-bizonyíték (D8) — nem verziókezeljük.
test-runs/

# A mermaid-filter (az export-doc.py pandoc-futtatása) a cwd-be írja ezt a
# 0 bájtos hibanaplót. Melléktermék, nem verziókezeljük.
mermaid-filter.err
